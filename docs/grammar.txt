program =
  | print_stmt* EOF
  ;

print_stmt =
  | "print" expr ";"
  ;

expr =
  | add_expr
  ;

add_expr =
  | mul_expr ( ( "+" | "-" ) mul_expr )*
  ;

mul_expr =
  | prim_expr ( ( "*" | "/" | "%" ) prim_expr )*
  ;

prim_expr =
  | integer
  ;
